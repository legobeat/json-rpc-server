---
version: '3'
services:
  json-rpc-server:
    image: shardus/json-rpc-server:dev
    build:
      context: .
    restart: unless-stopped
    network_mode: host
    configs:
      - source: archiverconfig
        target: /app/archiverConfig.json

configs:
  archiverconfig:
    file: ./archiverConfig.json
